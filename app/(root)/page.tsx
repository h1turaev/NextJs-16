import Home from "@/_components/homepage/page";

/**
 * `/` marshruti. Next.js 16: page.tsx — Server Component (default),
 * marshrutga xos UI alohida komponentga ajratiladi.
 */
export default function Page() {
  return <Home />;
}
